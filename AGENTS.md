# Content Rabbit Claude Code plugin

Before public copy or interface work, read `.agents/brand.md` and
`.agents/design.md`.

## Mental model

This repository connects Claude Code to Content Rabbit's shared MCP tools.
It owns plugin, marketplace, and MCP registry metadata.

It does not own the hosted Content Rabbit site or API deployment.

## Verification

Run `node --test` after changing metadata or instructions.
Keep credentials out of tracked files and examples.
